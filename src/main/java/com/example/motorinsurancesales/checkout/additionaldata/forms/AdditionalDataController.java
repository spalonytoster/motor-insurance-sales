package com.example.motorinsurancesales.checkout.additionaldata.forms;

// does it even need to have its own package? this should be a really small module.
// let's just keep it simple until some business rules happen or need for domain events...
// for now im keeping this package only as a hint
class AdditionalDataController {
}
